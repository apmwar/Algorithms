#include <iostream>
#include <limits>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    int T;
    cin >> T;
    while(T--){
        int n, R;
        cin >> n >> R;
        vector<long int> rat(n);
        long int least, most;
        for(int i = 0; i < n; i++)
            cin >> rat[i];
        for(int i = 1; i < n - 2; i++){

        }
    }
    return 0;
}
